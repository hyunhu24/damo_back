# 배포(Railway) 실행 환경 설정
spring:
  jackson:
    time-zone: Asia/Seoul

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  # 정적 리소스 서빙은 WebMvcConfig 의 /images/** 핸들러가 담당.
  # static-locations 를 file:/file-assets/ 로 덮어쓰면 classpath:/static(기본 이미지)이 가려지므로 오버라이드하지 않음.

  # AWS SDK 자동설정 제외 (S3 미사용 → EC2 메타데이터 조회로 인한 기동 지연/실패 방지)
  autoconfigure:
    exclude:
      - org.springframework.cloud.aws.autoconfigure.context.ContextCredentialsAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextInstanceDataAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextRegionProviderAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextResourceLoaderAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.context.ContextStackAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.messaging.MessagingAutoConfiguration
      - org.springframework.cloud.aws.autoconfigure.metrics.CloudWatchExportAutoConfiguration

  # Railway MySQL 플러그인 연결 (백엔드 서비스 Variables에 MYSQL* 참조 변수 등록 필요)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}

  jpa:
    # 컨트롤러/매퍼에서 지연로딩 연관관계(LAZY)에 접근하는 구조이므로 OSIV 활성화 필요.
    # false로 두면 트랜잭션 종료 후 매핑 단계에서 LazyInitializationException(500) 발생.
    open-in-view: true
    hibernate:
      ddl-auto: update   # 스키마 자동 생성/갱신, 데이터는 재시작해도 유지됨
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      platform: mysql      # data-mysql.sql 만 실행 (멱등 INSERT IGNORE)
      encoding: UTF-8
      continue-on-error: true

  # Redis: Railway에 Redis 플러그인 추가 후 REDIS_HOST/REDIS_PORT/REDIS_PASSWORD 주입. 미설정 시 localhost 폴백.
  # ※ 키는 spring.data.redis.* (RedisRepositoryConfig가 @Value로 직접 읽음)
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

springdoc:
  default-flat-param-object: true
  use-fqn: true
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true
    try-it-out-enabled: true

jwt:
  key: ${JWT_SECRET_KEY}
  access-token-expiration-minutes: 100000
  refresh-token-expiration-minutes: 240000

# StorageConfiguration이 주입받는 값들. S3 미사용이라 더미여도 됨.
cloud:
  aws:
    credentials:
      instance-profile: false
      accessKey: ${AWS_ACCESS_KEY:dummy}
      secretKey: ${AWS_SECRET_ACCESS_KEY:dummy}
    region:
      auto: false
      static: ap-northeast-2
    s3:
      bucket: ${AWS_BUCKET_NAME:dummy}
    stack:
      auto: false

file:
  # 업로드 저장 루트(절대경로). Railway 볼륨을 붙이려면 FILE_UPLOAD_DIR 로 마운트 경로 주입.
  # ※ Railway 컨테이너 파일시스템은 휘발성이라 볼륨 미사용 시 재배포마다 업로드 파일이 사라짐.
  upload-dir: ${FILE_UPLOAD_DIR:/app/file-assets}
  # 기본 이미지: classpath:/static/images/noImage.png 가 /images/noImage.png 로 서빙됨.
  default-image: /images/noImage.png

mail:
  address:
    admin: ${ADMIN_EMAIL:admin@google.com}

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN

server:
  port: ${PORT:8080}
  address: "::"   # Railway 내부 프록시가 IPv6로 접근하므로 IPv6 와일드카드로 바인딩
  servlet:
    encoding:
      force-response: true
  tomcat:
    threads:
      max: 30          # 기본 200 → 30으로 축소 (스레드 스택 메모리 절감)
      min-spare: 5
    max-connections: 200
